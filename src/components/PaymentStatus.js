import React from 'react';

const PaymentStatus = () => {
    return (
        <div>
            <h2>Payment Status</h2>
            {/* Show success or failure message based on the payment */}
        </div>
    );
};

export default PaymentStatus;
